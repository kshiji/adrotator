Adrotator
====================

Adrotator is a 2 part system.

1) A Webapp simple advertisement management system built on cakephp. The webapp exposes a json 
service that returns advertisment data.

2) A jQuery plugin that retrieves data from the json interface and renders advertisement in a webpage


